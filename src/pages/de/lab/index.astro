---
import Base from '../../../layouts/Base.astro';
import LabBody from '../../../components/LabBody.astro';
import { t } from '../../../i18n/translations';

const locale = 'de' as const;
const tr = t[locale];
---

<Base title={tr.pages.lab.title} description={tr.pages.lab.lead} locale={locale}>
  <LabBody locale={locale} />
</Base>
