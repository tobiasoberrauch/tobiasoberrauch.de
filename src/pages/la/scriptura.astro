---
import Base from '../../layouts/Base.astro';
import PillarPage from '../../components/PillarPage.astro';
import { t } from '../../i18n/translations';

const locale = 'la' as const;
const tr = t[locale];
---

<Base title={tr.pages.writing.title} description={tr.pages.writing.lead} locale={locale}>
  <PillarPage locale={locale} pillar="writing" />
</Base>
