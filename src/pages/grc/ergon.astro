---
import Base from '../../layouts/Base.astro';
import PillarPage from '../../components/PillarPage.astro';
import { t } from '../../i18n/translations';

const locale = 'grc' as const;
const tr = t[locale];
---

<Base title={tr.pages.working.title} description={tr.pages.working.lead} locale={locale}>
  <PillarPage locale={locale} pillar="working" />
</Base>
